{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "//": [
    "Config for the APPLICATION project (app.talyn.dev, VERCEL_PROJECT_ID_APP).",
    "",
    "It lives at the REPO ROOT, not in apps/web, because Vercel reads",
    "vercel.json from the project's Root Directory — and this project's Root",
    "Directory must be the repo root so the build can run npm workspace",
    "commands (@talyn/web depends on @talyn/shared and @talyn/client, which",
    "have to be compiled first; `npm run build -w …` only works from the",
    "workspace root).",
    "",
    "The marketing WEBSITE project is unaffected: its Root Directory is",
    "apps/marketing, so it reads apps/marketing/vercel.json (which does not",
    "exist) and never sees this file."
  ],

  "buildCommand": "npm run build -w @talyn/shared && npm run build -w @talyn/client && npm run build -w @talyn/web",
  "outputDirectory": "apps/web/dist",
  "framework": null,

  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],

  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://*.githubusercontent.com; font-src 'self'; connect-src 'self' https://prod.talyn.dev wss://prod.talyn.dev https://*.supabase.co wss://*.supabase.co https://us.i.posthog.com; frame-ancestors 'none'; base-uri 'none'; object-src 'none'; form-action 'none'"
        },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        {
          "key": "Strict-Transport-Security",
          "value": "max-age=63072000; includeSubDomains; preload"
        }
      ]
    },
    {
      "source": "/index.html",
      "headers": [{ "key": "Cache-Control", "value": "no-store" }]
    }
  ]
}
