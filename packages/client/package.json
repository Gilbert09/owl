{
  "name": "@talyn/client",
  "version": "0.1.0",
  "description": "Talyn backend client — REST + WebSocket transport shared by every front end",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@talyn/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
